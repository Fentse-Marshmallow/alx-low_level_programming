#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point
 *
 * Description: Print letters in lower and upper case
 *
 * Return 0 (Success)
 */

int main(void)
{
	int l;

	l = 'a';
	while
		(l <= 'z')
		{
			putchar(l);
			l++;
		}

	putchar('\n');
	return (0);
}

